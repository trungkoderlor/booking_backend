extends ../../layouts/auth.pug
include ../../mixins/alert.pug

block main
  +alert-error(expressFlash,5000)
  +alert-success(expressFlash,5000)


  .container.my-5
    .row.justify-content-center
      .col-8.col-md-6.col-lg-4
        .card.shadow-lg
          .card-body
            h1(class="text-center mb-4") Đăng nhập
            form(
              action=`${prefixAdmin}/auth/login`
              method="POST"
            )
              div.email-form(class="form-group")
                label(for="email") Email
                input(
                  type="email"
                  class="form-control"
                  id="email"
                  name="email"
                  placeholder="Nhập email của bạn"
                  required
                )
              div.pass-form(class="form-group")
                label(for="password") Mật khẩu
                input(
                  type="password"
                  class="form-control"
                  id="password"
                  name="password"
                  placeholder="Nhập mật khẩu của bạn"
                  required
                )
              div.btn-login(class="form-group text-center")
                button(
                  type="submit"
                  class="btn btn-primary btn-block"
                ) Đăng nhập
